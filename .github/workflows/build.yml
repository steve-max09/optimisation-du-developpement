name: Main CI workflow
on:
  push:
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Quality step
      - run: echo “phpstan”
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Build project
        run: echo "Build"